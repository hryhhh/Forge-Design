import { Upload } from './Upload'
import type {
  UploadFile,
  UploadStatus,
  UploadProgress,
  UploadProps,
} from './type'

export { Upload }

export type { UploadFile, UploadStatus, UploadProgress, UploadProps }
